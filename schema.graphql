# TODO: It's taking the account deployed from as ID which obviously shouldn't be the case. Solve.
type Kali @entity {
  id: ID!
  daoAddress: Bytes!
  name: String!
}

type User @entity {
  id: ID!
  kali: Kali!
  userAddress: Bytes!
}
