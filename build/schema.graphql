type DAO @entity {
  id: ID!
  daoAddress: Bytes!
  name: String!
  members: [User!]!
}

type User @entity {
  id: ID!
  daos: [DAO!]! @derivedFrom(field: "members")
}

# type UserDAO @entity {
#   id: ID!
#   user: User!
#   dao: DAO!
# }
